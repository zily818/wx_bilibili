<view class="main">
  <!-- 公共头部 -->
  <MyTile></MyTile>

  <!-- 首页导航 -->
  <view class="nav_wrap">
    <!-- 滚动组件 -->
    <scroll-view class="nav" scroll-x="{{true}}">
      <view bindtap="activeNav" data-index="{{index}}" 
      class="nav_item {{index===currentIndexNav?'active':''}}" 
      wx:for="{{navList}}" wx:key="index">
        {{item.text}}
      </view>
    </scroll-view>
  </view>

  <!-- 轮播图 -->
  <view class="slides">
    <swiper autoplay indicator-dots circular >
      <swiper-item wx:for="{{swiperList}}" wx:key="index" >
        <navigator >
          <image mode="widthFix" src="{{item.imgSrc}}"  />
        </navigator>
      </swiper-item>
    </swiper>
  </view>

<!-- 视频列表 -->
  <view class="video_wrap">
    <navigator url="../detail/detail?id={{item.id}}" class="video_item" wx:for="{{videosList}}" wx:key="index" >
      <!-- 图片容器 -->
      <view class="video_img">
        <!-- 图片 -->
        <image mode="widthFix" src="{{item.imgSrc}}" />
        <!-- 播放量 .....-->
        <view class="video_info">
          <!-- 左侧为播放量 -->
          <view class="play_count_wrap">
            <!-- 图标 -->
            <image style="width:40rpx;height:40rpx;" src="../../icons/view.png" />
            <!-- 数值 -->
            <text class="play_count">{{item.playCount}}</text>
          </view>
          <!-- 右侧为评论量 -->
          <view class="comment_count_wrap"></view>
          <!-- 图标 -->
            <image style="width:40rpx;height:40rpx;" src="../../icons/comment.png" />
            <!-- 数值 -->
            <text class="comment_count">{{item.commentCount}}</text>
        </view>
      </view>
      <!-- 标题 -->
      <view class="video_title">{{item.desc}}</view>
    </navigator>
  </view>
</view>